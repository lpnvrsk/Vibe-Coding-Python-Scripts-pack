# Vibe Coding Python Scripts Pack

Набор независимых Python-скриптов для различных задач. Каждый скрипт выполняет отдельную функцию и может использоваться независимо от других.

## 📋 Содержание

- [ImageAnylizerViaBLIPandEasyOCR.py](#imageanylizerviablipandeasyocrpy)
- [mp3_antidub.py](#mp3_antidubpy)
- [MulticonverterForVideosToUploadtoTG.py](#multiconverterforvideostouploadtotgpy)
- [TelegramChatExportNameDepersonalizer.py](#telegramchatexportnamedepersonalizerpy)
- [TextMorpher.py](#textmorpherpy)

---

## ImageAnylizerViaBLIPandEasyOCR.py

**Анализ изображений с помощью BLIP и EasyOCR**

Скрипт анализирует изображения в указанной папке и создает текстовое описание для каждого файла. Использует две модели:
- **BLIP** (Salesforce) - для генерации текстового описания содержимого изображения
- **EasyOCR** - для извлечения текста из изображений (поддерживает русский и английский языки)

### Возможности:
- Автоматическая обработка всех изображений в папке
- Поддержка форматов: PNG, JPG, JPEG, BMP, TIFF, WEBP
- Генерация описаний с помощью нейросети
- Распознавание текста на изображениях
- Поддержка GPU для ускорения обработки
- Сохранение результатов в текстовый файл

### Требования:
```bash
pip install torch torchvision transformers easyocr pillow
```

### Использование:
```bash
python ImageAnylizerViaBLIPandEasyOCR.py
```
После запуска скрипт запросит путь к папке с изображениями. Результаты будут сохранены в файл `описания_картинок.txt`.

---

## mp3_antidub.py

**Утилита для очистки и организации MP3 коллекции**

Скрипт помогает навести порядок в коллекции MP3 файлов:
- Находит и удаляет дубликаты файлов (по MD5 хешу)
- Организует музыку по исполнителям (автоматически извлекает имя исполнителя из имени файла)
- Перемещает треки исполнителей с более чем 4 треками в отдельные папки

### Возможности:
- Поиск дубликатов по хешу файлов
- Автоматическое определение исполнителя из имени файла
- Организация файлов по папкам исполнителей
- Режим "dry run" для предварительного просмотра изменений
- Детальная статистика и отчеты

### Использование:

**Режим предварительного просмотра (dry run):**
```bash
python mp3_antidub.py "путь/к/папке/с/музыкой"
```

**Выполнение изменений:**
```bash
python mp3_antidub.py "путь/к/папке/с/музыкой" --apply
```

### Особенности:
- Скрипт сохраняет первый найденный файл из группы дубликатов, остальные удаляет
- Исполнители с 4 или менее треками остаются в общей папке
- Файлы с неопределенным исполнителем (Unknown_Artist) не перемещаются
- Перед выполнением изменений запрашивается подтверждение

---

## MulticonverterForVideosToUploadtoTG.py

**Конвертер видео для загрузки в Telegram**

Скрипт конвертирует видео файлы в формат MP4 с параметрами, оптимизированными для загрузки в Telegram. Обрабатывает все видео файлы в текущей директории и сохраняет результаты в папку `converted`.

### Поддерживаемые форматы:
MP4, MOV, AVI, MKV, FLV, WMV, WEBM, M4V

### Параметры конвертации:
- Видео кодек: H.264 (libx264)
- Preset: medium (баланс скорость/качество)
- CRF: 23 (качество)
- Аудио кодек: AAC
- Битрейт аудио: 128k
- Faststart: включен (для быстрой перемотки в браузерах)

### Требования:
Требуется установленный `ffmpeg` и добавленный в PATH.

### Использование:
```bash
python MulticonverterForVideosToUploadtoTG.py
```
Скрипт автоматически найдет все видео файлы в текущей директории и конвертирует их в папку `converted`.

---

## TelegramChatExportNameDepersonalizer.py

**Деперсонификация экспортированных чатов Telegram**

Скрипт обрабатывает HTML файлы экспорта чатов Telegram (формат `messages*.html`) и создает обезличенную версию всех сообщений. Заменяет реальные имена пользователей на анонимные идентификаторы (author1, author2, и т.д.).

### Возможности:
- Обработка нескольких HTML файлов экспорта
- Замена имен пользователей на анонимные идентификаторы
- Удаление эмодзи (конвертация в текстовое представление)
- Сохранение всех сообщений в один Markdown файл
- Сохранение структуры сообщений

### Требования:
```bash
pip install beautifulsoup4 emoji
```

### Использование:
1. Поместите HTML файлы экспорта (формат `messages*.html`) в ту же папку, что и скрипт
2. Запустите скрипт:
```bash
python TelegramChatExportNameDepersonalizer.py
```

Результат будет сохранен в файл `AllMessages.md`.

### Формат результата:
```
author1: Текст первого сообщения
author2: Текст второго сообщения
author1: Еще одно сообщение от первого автора
...
```

---

## TextMorpher.py

**Генератор случайного текста**

Скрипт преобразует введенный текст, заменяя каждый символ на случайный символ того же типа, сохраняя структуру текста:
- Буквы заменяются на случайные буквы (с сохранением регистра)
- Цифры заменяются на случайные цифры
- Специальные символы заменяются на случайные специальные символы
- Пробелы остаются без изменений

### Возможности:
- Сохранение структуры текста (регистр, пробелы, типы символов)
- Полная рандомизация содержимого символов

### Использование:
```bash
python TextMorpher.py
```
После запуска введите текст для преобразования.

### Пример:
```
Вход: Hello World 123!
Выход: Xkfqp Vjgtr 847@
```

---

## Лицензия

MIT License

Copyright (c) 2025 lpnvrsk

---

## Примечания

- Все скрипты независимы друг от друга
- Каждый скрипт имеет свои зависимости, указанные в описании
- Рекомендуется использовать виртуальное окружение Python для управления зависимостями

